# Today's PLANNED day shifts (one per machine per site-local date, HLD §6.6). The window is
# SHIFT_START/SHIFT_END (infra/.env.example); seed.py resolves the date at seed time.
# EXC002 / OP1002 is "Operator B" for the demo beat HLD §12 3:45–4:05 (drives into the
# worker zone Operator A reported).
- machine_id: EXC001
  operator_id: OP1001
- machine_id: EXC002
  operator_id: OP1002
