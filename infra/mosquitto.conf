# Demo LAN broker. Anonymous access is allowed ON PURPOSE for the isolated travel-router LAN
# (plan.md §2). Do not expose this broker on an untrusted network.
listener 1883
allow_anonymous true
# Persist retained messages (cat/{site}/hazards = site memory) across broker restarts.
persistence true
persistence_location /mosquitto/data/
log_dest stdout
